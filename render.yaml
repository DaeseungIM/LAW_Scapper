services:
  - type: web
    name: law-scrapper-backend
    env: python
    buildCommand: "./build.sh"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: LAW_API_KEY
        value: ceiai_law_test
